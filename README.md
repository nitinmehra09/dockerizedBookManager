# dockerizedBookManager
Showcasing Docker skills: Production-ready containerization of a full-featured book management application (React + Tailwind + Docker). Includes optimized Dockerfile, .dockerignore, and deployment configuration.
